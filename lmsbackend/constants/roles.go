package constants

// User roles
const (
	RoleCreator = "creator"
	RoleBrand   = "brand"
)

// OTP settings
const (
	OTPLength = 6
	OTPExpiry = 5 // minutes
)

// JWT
const (
	TokenExpiryHours = 24
)
